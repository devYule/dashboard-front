import { WidgetInter } from "../../../interfaces/Interfaces";


export default function Widget({ widget }: { widget: WidgetInter }) {

    // const sizeClassName: string | undefined = widget.isShown ? 'widget-item' : undefined;

    // todo 위젯 알고리즘 작성

    return (
        <div className="widget">
            
        </div>
    );
}